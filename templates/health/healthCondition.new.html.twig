{% extends "base.html.twig" %}

{% block title %}Ajouter une particularité{% endblock %}

{% block body %}

<h1>Ajouter votre maladie, allergie ou handicap</h1>

<div class="row health-small-form">
  <div class="col s12">
    <div class="row">
      <div class="input-field col s12">
        {{ form_start(form) }}
        {{ form_label(form.title) }}
        {{ form_widget(form.title) }}
        <br>
        <br>
        {{ form_widget(form.submit) }}
        {{ form_end(form) }}
      </div>
    </div>
  </div>
</div>

{% endblock %}