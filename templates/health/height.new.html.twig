{% extends "base.html.twig" %}

{% block title %}Ajouter taille{% endblock %}

{% block body %}

<h1>Ajouter votre taille</h1>

<div class="row">
  <div class="col s12">
    <div class="row">
      <div class="input-field col s12">
        {{ form_start(form) }}
        {{ form_widget(form.value) }}
        <br>
        <br>
        {{ form_widget(form.submit) }}
        {{ form_end(form) }}
      </div>
    </div>
  </div>
</div>

{% endblock %}