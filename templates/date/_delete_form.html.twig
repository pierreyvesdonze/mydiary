<form method="post" action="{{ path('date_delete', {'id': date.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ date.id) }}">
    <button class="custom-btn btn-danger">Delete</button>
</form>
