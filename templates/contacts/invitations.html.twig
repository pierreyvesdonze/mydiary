<li>
    {% if friendRequests %}
    <div class="collapsible-header">
        <h4>Invitations en cours ({{ friendRequests|length }})</h4>
    </div>
    <div class="collapsible-body">
        {% for friendRequest in friendRequests %}
        <div>
            <strong>{{ friendRequest.sender.pseudo }}</strong>
            <a href="{{ path('delete_friendship_request', {"user2":friendRequest.sender.id}) }}" class="secondary-content">Refuser</a>
            <a href="{{ path('create_friendship', {'sender':friendRequest.sender.id}) }}" class="secondary-content">Accepter</a>
        </div>
        <br>
        {% endfor %}
    </div>
    {% else %}
    <div class="collapsible-header">
        <h5>Vous n'avez pas d'invitation pour le moment</h5>
    </div>
    {% endif %}
</li>